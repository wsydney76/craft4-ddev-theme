{% js %}
function isIE() {
    const ua = window.navigator.userAgent; //Check the userAgent property of the window.navigator object
    const msie = ua.indexOf('MSIE '); // IE 10 or older
    const trident = ua.indexOf('Trident/'); //IE 11

    return (msie > 0 || trident > 0);
}

if (isIE()) {
    warning = document.getElementById('ie-warning');
    warning.style.display = '';
}

{% endjs %}
<div id="ie-warning" class="p-2 w-full text-center text-ls"
     style="background-color: maroon; color:white; display: none;">
    {% if currentSite.language[:2] ==  'de'%}
        Internet Explorer wird nicht unterstützt und zeigt unsere Website nicht richtig an. Bitte benutzen Sie einen modernen Browser.
        {% else %}
        Internet Explorer is not supported and does not show our website correctly. Please use a modern browser.
    {% endif %}

</div>
