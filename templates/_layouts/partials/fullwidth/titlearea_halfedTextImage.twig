<header class="flex flex-col-reverse lg:flex-row mb-16">
    <div class="lg:w-1/2 bg-title-bg dark:bg-title-bg-dark text-title-text dark:text-title-text-dark px-12 lg:flex lg:flex-col lg:justify-end pb-4 lg:pb-12 xl:pb-24">
        <div class="">

            <div class="mt-8 lg:mt-0">
                {% include '_layouts/partials/fullwidth/includes/meta.twig' %}
            </div>

            <div class="mt-2 text-base">
                {{ entry.teaser }}
            </div>

            <h1 class="text-4xl lg:text-3xl xl:text-4xl mt-1 py-2 tracking-wider">{{ entry.title }}</h1>
        </div>
    </div>
    <div class="lg:w-1/2">
        {{ titleImageHtml|raw }}
    </div>
</header>

